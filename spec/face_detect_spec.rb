require 'spec_helper'

describe FaceDetect do
  it 'has a version number' do
    expect(FaceDetect::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
